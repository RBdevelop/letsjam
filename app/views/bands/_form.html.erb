<%= simple_form_for(@band) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :band_name %>
    <%= f.input :genre %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
